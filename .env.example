# AI-Powered Code Suggestions and Team Dashboard
# Copy this file to .env and fill in your API keys

# Anthropic Claude API Key for AI-powered code suggestions and Replit Agent
# Get your API key from: https://console.anthropic.com/
# Required for: /api/analyze-code, /api/agent/* endpoints
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# GitHub Token for Team Dashboard functionality
# Generate a personal access token from: https://github.com/settings/tokens
# Required scopes: repo, read:org, read:user
GITHUB_TOKEN=your_github_token_here

# Database URL (if using a remote database)
# Required for session management and data persistence
DATABASE_URL=your_database_url_here

# Session Secret for secure session management
# Generate a random string for production
SESSION_SECRET=your_session_secret_here

# Replit Domains (required for Replit Auth)
REPLIT_DOMAINS=your-repl-domain.repl.co

# Replit ID (required for Replit Auth)
REPL_ID=your_repl_id

# Issuer URL (optional, defaults to Replit's OIDC)
ISSUER_URL=https://replit.com/oidc

# Allowed origins for CORS (production)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com